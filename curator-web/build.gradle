apply plugin: 'war'

sourceCompatibility = 1.8
version = '1.0'

war {
    baseName = "curator"
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}